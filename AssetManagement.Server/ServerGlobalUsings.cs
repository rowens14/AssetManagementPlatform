/*
 * FILE: ServerGlobalUsings.cs
 * PROJECT: AssetManagement.Server
 * PURPOSE: Global using directives for the Server project.
 *          In in-memory mode this file is intentionally minimal — Npgsql is not
 *          referenced since there is no database connection. When switching back
 *          to PostgreSQL mode, re-add: global using Npgsql;
 */

// No additional global usings required for in-memory mode.
